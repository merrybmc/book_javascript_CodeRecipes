const date = new Date();

const locale1 = date.toLocaleString();
const locale2 = date.toLocaleDateString();
const locale3 = date.toLocaleTimeString();
console.log(locale1);
console.log(locale2);
console.log(locale3);
