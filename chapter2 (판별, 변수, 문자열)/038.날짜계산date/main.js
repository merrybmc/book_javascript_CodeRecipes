const userName = "고양이";

const tag = '<div class="container">' + "안녕하세요" + userName + "님" + "</div>";

console.log(tag);
const temp = 123;
const str2 = `123 x 123은 ${temp * 123} 입니다.`;
console.log(str2);
